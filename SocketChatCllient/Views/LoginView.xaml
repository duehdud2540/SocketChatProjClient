<UserControl x:Class="SocketChatClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SocketChatClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Width="340" 
                Padding="30" 
                Background="#FFFFFF" 
                CornerRadius="10" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="2" Opacity="0.15"/>
            </Border.Effect>
            
            <StackPanel>
                <TextBlock Text="Socket Chat" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="#212529" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,25"/>

                <TextBlock Text="{Binding ErrorMessage}"
               Foreground="#DC3545"
               FontSize="12"
               HorizontalAlignment="Center"
               Margin="0,0,0,10"
               TextWrapping="Wrap"/>
                <TextBlock Text="아이디" Foreground="#495057" Margin="0,0,0,5"/>
                <TextBox Height="36" Margin="0,0,0,15" VerticalContentAlignment="Center" Padding="5,0"
                    Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="비밀번호" Foreground="#495057" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PwBox" Height="36" Margin="0,0,0,25" VerticalContentAlignment="Center" Padding="5,0"/>
                <TextBlock HorizontalAlignment="Center" Margin="0,0,0,15">
                <Run Text="계정이 없으신가요? " Foreground="#6C757D"/>
                <!-- 하이퍼링크 요소 -->
    <Hyperlink Command="{Binding NavigateToRegisterCommand}"
               Foreground="#0D6EFD"
               TextDecorations="None">
        <!-- 마우스 호버(Hover) 시 밑줄 스타일 -->
        <Hyperlink.Style>
            <Style TargetType="Hyperlink">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="TextDecorations" Value="Underline"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Hyperlink.Style>
        회원가입
    </Hyperlink>
                </TextBlock>
                <TextBlock HorizontalAlignment="Center" Margin="0,0,0,15">
                <Run Text="계정을 잊으셨나요? " Foreground="#6C757D"/>
                <!-- 하이퍼링크 요소 -->
    <Hyperlink Command="{Binding NavigateToRegisterCommand}"
               Foreground="#0D6EFD"
               TextDecorations="None">
        <!-- 마우스 호버(Hover) 시 밑줄 스타일 -->
        <Hyperlink.Style>
            <Style TargetType="Hyperlink">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="TextDecorations" Value="Underline"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Hyperlink.Style>
        아이디/비밀번호찾기
    </Hyperlink>
                </TextBlock>

                <Button Content="로그인" 
                        Height="40" 
                        Background="#0D6EFD" 
                        Foreground="White" 
                        FontWeight="Bold" 
                        Cursor="Hand"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=PwBox}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
